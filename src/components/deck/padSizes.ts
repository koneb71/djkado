/** Beat sizes for the loop-roll and beat-jump pad modes (index = pad number). */
export const ROLL_SIZES = [1 / 16, 1 / 8, 1 / 4, 1 / 2, 1, 2, 4, 8];
export const JUMP_SIZES = [-16, -8, -4, -1, 1, 4, 8, 16];
/** 4-pad (phone) variants: the most useful subset of each list. */
export const ROLL_SIZES_4 = [1 / 8, 1 / 4, 1 / 2, 1];
export const JUMP_SIZES_4 = [-4, -1, 1, 4];
